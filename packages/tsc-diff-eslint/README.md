# eslint-tsc-staged

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build eslint-tsc-staged` to build the library.

## Running unit tests

Run `nx test eslint-tsc-staged` to execute the unit tests via [Jest](https://jestjs.io).
